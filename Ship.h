#ifndef SHIP_H_
	#define SHIP_H_

#include "CEntity.h"

class Ship : public CEntity {

	public:

	//virtual bool OnLoad(char* File, int Width, int Height, int MaxFrames);
	virtual bool OnLoad();

};

#endif
